\c one_bar;
DROP TABLE IF EXISTS user_info;
CREATE TABLE user_info (
  number VARCHAR(255) PRIMARY KEY,
  name VARCHAR(255),
  
  );

INSERT INTO user_info (number, name)
VALUES('+12068497029', 'el Jaredito');
INSERT INTO user_info (number, name)
VALUES('+12533101489', 'Andrew');
